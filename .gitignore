.DS_Store
.env
.env.local
*.log
__pycache__/
*.pyc
.venv/
venv/
.terraform/
*.tfstate
*.tfstate.backup
*.sarif
# The actions also drop a .json alongside each .sarif in these directories.
orca-*-results/

# Terragrunt / Atlantis plan artifacts. orca-plan.json is a rendered plan and
# can contain sensitive input values in plaintext — never commit one.
.terragrunt-cache/
*.tfplan
orca-plan.json
